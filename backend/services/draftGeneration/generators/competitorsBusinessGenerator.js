/**
 * Business-competitors generator — STUB.
 *
 * Produces visibility_profiles.competitors_business (ordered list of direct
 * business competitors). Not automated yet; yields an empty list.
 */

const { makeStub } = require('./_interface');

module.exports = makeStub('competitors_business', { competitors_business: [] });
