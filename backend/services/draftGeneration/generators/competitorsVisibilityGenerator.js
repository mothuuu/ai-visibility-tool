/**
 * Visibility-competitors generator — STUB.
 *
 * Produces visibility_profiles.competitors_visibility (ordered list of
 * competitors that surface in AI/search answers — "visibility" competitors,
 * classified separately from business competitors). Not automated yet; yields
 * an empty list.
 */

const { makeStub } = require('./_interface');

module.exports = makeStub('competitors_visibility', { competitors_visibility: [] });
